name: CI

on:
  merge_group:
    branches: [main]
  push:
    branches: [main]

jobs:
  examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and run all examples
        run: ./test.sh
